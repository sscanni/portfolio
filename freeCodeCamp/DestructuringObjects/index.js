function getLength(str) {
    "use strict";

    const { length : len } = str;  //Get the length of str and put the value into len.
  
    return len; 
  
  }
  
  console.log(getLength('FreeCodeCamp'));
